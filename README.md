# Правила Site-02: Classic

Публичная страница с полными правилами сервера SCP: Secret Laboratory.

После публикации она будет доступна по адресу:

```text
https://smokyplay.github.io/site-02-rules/
```

Текст правил находится в `index.html`, оформление — в `styles.css`.
Изменения, отправленные в ветку `main`, автоматически публикуются через GitHub Pages.
